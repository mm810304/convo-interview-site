import React from 'react';

const SentencePatternIndexPage = () => {
    return (
        <div>
            List of all lessons that will include only sentence pattern part.  Top of page will have links to jump around easily to different categories.
            Also, all lessons will be on one page, but there will be header titles for the sections to make it easier to find.
        </div>
    );
};

export default SentencePatternIndexPage;